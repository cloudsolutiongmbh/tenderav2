# Copy to packages/backend/.env.local

# Convex local/dev deployment
# Obtain from `npx convex dev` output or your Convex dashboard
CONVEX_DEPLOYMENT=
CONVEX_URL=

# Clerk JWT issuer for Convex auth (from your Clerk instance)
CLERK_JWT_ISSUER_DOMAIN=

# LLM configuration (used by packages/backend/convex/llm.ts)
# Provider must be one of: OPENAI | ANTHROPIC
LLM_PROVIDER=
# Example: gpt-4o-mini (OpenAI) or claude-3-5-sonnet (Anthropic)
LLM_MODEL=

# API keys (set only the one matching your provider)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Backend-side upload limit (MB). Web uses VITE_MAX_UPLOAD_MB/MAX_UPLOAD_MB for UI.
MAX_UPLOAD_MB=200

